type FormType = {
    name: string, 
    textarea?: boolean, 
    editor?: boolean, 
    file?: boolean,
    placeholder?: string | number,
    type?: string; 
    subtitle?: string, 
};

export default FormType; 